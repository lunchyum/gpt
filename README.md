# Android Example App

간단한 Android 예제 앱입니다.

## 기능
- 기본 화면 표시
- 버튼 클릭 횟수 증가
- GitHub Actions에서 Debug APK 자동 빌드

## 실행
Android Studio에서 `AndroidExampleApp` 폴더를 열고 Gradle Sync 후 `app` 실행 구성을 선택하세요.

APK는 GitHub Actions의 `android-example-debug-apk` artifact로 생성됩니다.
